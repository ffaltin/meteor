<section class="page" id="contact">
	<section class="map" id="map-wrapper"><div id="map" data-map="{{ page.contact.get_field("address") }}"></div></section>
	<section class="description">
		<div>
			<h1>{{ page.contact.title }}</h1>
			{{ page.contact.content }}

			<form action="#" method="post">
			    {{ form_widget(form) }}
			    <input type="submit" value="{{ __("Nous contacter") }}" />
			</form>
		</diV>
	</section>
</section>
